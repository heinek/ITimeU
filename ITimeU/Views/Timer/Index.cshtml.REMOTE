@model ITimeU.Models.TimerModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<input type="button" id="btnStart" value="Start" onclick="location.href='@Url.Action("StartTimer", "Timer")'" />
@if (Model.IsStarted) { <p>Timer is started! Start time: @Model.StartTime </p> }
        
    
    