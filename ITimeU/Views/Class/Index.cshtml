@model IEnumerable<ITimeU.Models.AthleteClassModel>
@{
    ViewBag.Title = "Index";
}
<div class="topmenubar">
    <center>
        | @Html.ActionLink("HJEM", "Index", "Home", null, new { @class = "topmenulink" })
        | @Html.ActionLink("ADMIN", "Index", "Admin", null, new { @class = "topmenulink" })
        |
    </center>
</div>
<div class="contentmargin">
    <h2>
        Klasser</h2>
    <p>
        @Html.ActionLink("Opprett ny klasse", "Create")
    </p>
    <table>
        <tr>
            <th>
                Name
            </th>
            <th>
            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Name
                </td>
                <td align="right">
                    @Html.ActionLink("Endre", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Slett", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </table>
</div>
